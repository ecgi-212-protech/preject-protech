#include <iostream>
#include <windows.h>

using namespace std;

int main()
{


    string hello = "Hello Bob. Let's get\nprogramming in C++";
    int x=0;
    while ( hello[x] != '\0')
    {
        cout << hello[x];
        if ( hello[x]!= ' ' && hello[x]!= '\n')
		Beep(850, 300);
        Sleep(100);
        x++;
    };
    cout << "\n\nEnd of message.." << endl << endl;
    return 0;

}



Using here

    string hello = "Put trash in the right bin\n----How to play----\n- Using arrow keys to move the trash into the right bin\n- Score will be added every time you put your trash into a right bin\n- If you missed or put your trash in a wrong bin, Your goodness(life) will decrease\n- You have 3 life before game over\n******* Let's go! (^v^)/ *******\n";
    int x=0;
    while ( hello[x] != '\0')
    {
        cout << hello[x];
        if ( hello[x]!= ' ' && hello[x]!= '\n')
        Sleep(75);
        x++;
    };
    cout << "\n\nEnd of message.." << endl << endl;
    return 0;
